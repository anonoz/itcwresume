<nav class="navbar navbar-default navbar-employer">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "ITCW for Employer", resumes_path, class: "navbar-brand" %>
    </div>

    <div class="navbar-collapse collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if current_page?(full_time_resumes_path) %>">
          <%= link_to "Full-time (#{Resume.full_time.count})", full_time_resumes_path %>
        </li>
        <li class="<%= 'active' if current_page?(internship_resumes_path) %>">
          <%= link_to "Internship (#{Resume.internship.count})", internship_resumes_path %>
        </li>
        <li class="<%= 'active' if current_page?(contact_us_path) %>">
          <%= link_to "Contact Us", contact_us_path %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to "Logout", destroy_employer_session_path, method: :delete %>
        </li>
      </ul>
      <form class="navbar-form navbar-right" method="GET" action="/resumes/search">
        <div class="form-group">
          <input type="tel" name="student_id" class="form-control" placeholder="MMU Student ID" pattern="\d{10}" required>
          <button type="submit" class="btn btn-default">Open Resume</button>
        </div>
      </form>
    </div>
  </div>
</nav>
