<nav class="navbar navbar-default navbar-employer">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "IJE for Employer", resumes_path, class: "navbar-brand" %>
    </div>

    <div class="navbar-collapse collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if current_page?(inbox_resumes_path) %>">
          <%= link_to inbox_resumes_path do %>
            Inbox <span class="label label-default"><%= @navbar_resume_counts[0] %></span>
          <% end %>
        </li>
        <li class="<%= 'active' if current_page?(starred_resumes_path) %>">
          <%= link_to starred_resumes_path do %>
            Starred <span class="label label-default"><%= @navbar_resume_counts[1] %></span>
          <% end %>
        </li>
        <li class="<%= 'active' if current_page?(completed_resumes_path) %>">
          <%= link_to completed_resumes_path do %>
            Completed <span class="label label-default"><%= @navbar_resume_counts[2] || 0 %></span>
          <% end -%>
        </li>
        <li class="<%= 'active' if current_page?(ignored_resumes_path) %>">
          <%= link_to ignored_resumes_path do %>
            Ignored <span class="label label-default"><%= @navbar_resume_counts[3] || 0 %></span>
          <% end -%>
        </li>
        <li class="<%= 'active' if current_page?(contact_us_path) %>">
          <%= link_to "Contact Us", contact_us_path %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to "Logout", destroy_employer_session_path, method: :delete %>
        </li>
      </ul>
      <form class="navbar-form navbar-right" method="GET" action="/resumes/search">
        <div class="form-group">
          <input type="tel" name="student_id" class="form-control" placeholder="Student ID" title="MMU Student IDs are 10 digits long" required>
          <button type="submit" class="btn btn-default">Open Resume</button>
        </div>
      </form>
    </div>
  </div>
</nav>
